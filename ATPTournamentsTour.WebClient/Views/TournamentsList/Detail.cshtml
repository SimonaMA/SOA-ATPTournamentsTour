@model ATPTournamentsTour.WebClient.Models.Api.Tournament
<div class="row mt-5">
    <div class="col-md-4">
        <img src="@Model.ImageUrl" class="detailImage" alt="PastChampion pic" />
    </div>
    <div class="col-md-6 ml-1">
        <div class="row">
            <div class="columnHeader">@Model.CategoryName.ToUpper()</div>
        </div>
        <div class="row mt-2 bold">
            <h1>@Model.TournamentName</h1>
        </div>
        <div class="row mt-2">
            <h3>@Model.PastChampion</h3>
        </div>
        <div class="row mt-3 mt-4 bold">
            <h3>@Model.Date.ToShortDateString()</h3>
        </div>
        <div class="row mt-4">
            @Model.TournamentDescription
        </div>
        <div class="row mt-5">
            <h3><span class="bold">$@Model.TicketPrice</span></h3><span class="mt-1">&nbsp;&nbsp;&nbsp; PER TICKET</span>
        </div>
    </div>
</div>
<div class="row mt-5">
    <div class="col">
        <hr />
    </div>
</div>
<div class="row mt-4">
    <div class="col offset-4">
        <form class="form" asp-action="AddItem" asp-controller="Cart">
            <div class="row">
                <label class="columnHeader mt-2">QUANTITY:</label>
                <select name="TicketAmount" class="ml-5 w-25">
                    <option value="1">1</option>
                    <option value="2">2</option>
                    <option value="3">3</option>
                    <option value="4">4</option>
                    <option value="5">5</option>
                </select>
                <input asp-for="TournamentId" type="hidden" />
                <input asp-for="TicketPrice" type="hidden" />
                <button type="submit" class="btn btn-primary ml-4">PLACE ORDER</button>
            </div>
        </form>
    </div>
</div>


